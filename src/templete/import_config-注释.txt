{
  //本文件夹内通用的x，y偏移坐标
  "x": 208,
  "y": 252.4,
  "charfolder": {
    //本文件夹内，全局忽略的子文件夹
    "ignor": [
      "special",
      "svn"
    ],
    //全局使用的帧间隔，及每张图片占用的帧数，该数值也用来跳过图片，即每间隔interval数量的图片才导入一次库
    "interval": 2,
    //fla中需要建立的层名字，越靠后的层，在时间轴上出现的越靠上
    "layers": [
      "res",
      "label"
    ],
    //label组合连接符
    "join": "_",
    /*每个子文件夹label的开始后缀和结束后缀
    通常label名字由文件夹名字通过 join 配置的连接符组合而成
    */
    "labels": {
      //文件夹名字
      "spell": {
        //组合label的时候，标记1的会忽略本文件夹的名字
        "ignor": 0,
        //起始label的后缀
        "start_suffix": "_start",
        //结束lebel的后缀
        "end_suffix": "_end",
        //插入标签的后缀
        "insert": [
          {
            "end_suffix": "_fire_point"
          }
        ]
      },
      "attack": {
        "ignor": 0,
        "start_suffix": "_start",
        "end_suffix": "_end"
      },
      "idskill": {
        "ignor": 1,
        "start_suffix": "_appear_start",
        "end_suffix": "_appear_end",
        "insert": [
          {
            "end_suffix": "_fire_point"
          }
        ]
      }
    },
    //所有子目录的详细配置
    "folders": {
      "grave": {
        //如果子目录下的图片x，y偏移值和通用的不一样，可以在这里配置
        "x": 10,
        "y": 10,
        //如果子目录的label名字和通用的规则不一样，可以在这里配置
        "label": "gravestone",
        //如果子目录的图片占用的帧数和通用的规则不一样，可以在这里配置
        "interval": 1,
        //用来保存该目录下所有图片的信息，由程序生成不需要配置
        "list": [],
        //该目录的起始、结束标签名字，由程序生成不需要配置
        "start_label": "_start",
        "end_label": "_end",
        //该目录插入标签的信息，如果不需要，可以不配置
        "insert": [
          {
            //在第几帧插入label，如果是0则不插入
            "frame": 0,
            //插入标签的名字，不需要配置，由程序根据通用 labels 中的 insert信息，组合而成
            //因此这两个配置如果要生效，需要同时出现
            "label": ""
          }
        ]
      },
      "idskill/30001/spell/down": {
        "x": 0,
        "y": 0,
        "label": "",
        "interval": 0,
        "list": [],
        "start_label": "_start",
        "end_label": "_end",
        "insert": [
          {
            "frame": 3,
            "label": ""
          }
        ]
      }
    },
    //原件的类名，由程序生成不需要配置
    "classname": "",
    //保存fla的名字，由程序生成不需要配置
    "flaname": ""
  }
}